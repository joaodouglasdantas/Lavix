services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: lavix
      POSTGRES_PASSWORD: lavix
      POSTGRES_DB: lavix_development
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lavix -d lavix_development"]
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    build: .
    command: bin/rails server -b 0.0.0.0
    volumes:
      - .:/app
      - bundle_cache:/usr/local/bundle
    ports:
      - "3000:3000"
    environment:
      RAILS_ENV: development
      DATABASE_URL: postgres://lavix:lavix@db:5432/lavix_development
    depends_on:
      db:
        condition: service_healthy
    tty: true
    stdin_open: true

  css:
    build: .
    command: bin/rails tailwindcss:watch
    volumes:
      - .:/app
      - bundle_cache:/usr/local/bundle
    depends_on:
      - web

  mailhog:
    image: mailhog/mailhog
    ports:
      - "1025:1025"   # SMTP — Rails envia para cá
      - "8025:8025"   # Interface web para ver os e-mails

volumes:
  postgres_data:
  bundle_cache:
